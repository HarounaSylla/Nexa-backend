# Ideas

Long-term ideas with no clear trigger yet. These are not backlog items
until something concrete makes them worth starting.

- **Customer memory tool** — deferred until the core loop is validated.
- **Split the TikTok classifier into its own service** — only if volume
  justifies the extra operational cost.
- **LLM gateway** — model routing and cost tracking, once more than one
  model path is actually in use.
- **Wolof language support** — pending a scope decision.
